# my-blog

마크다운 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트.

## 기술 스택

- 순수 HTML / CSS / JavaScript만 사용. 프레임워크나 빌드 도구(React, Vue, Next.js, Webpack 등) 사용 금지.
- 외부 라이브러리 의존은 최소화한다. 마크다운 파싱이 꼭 필요하면 CDN 없이 동작하는 경량 방식을 우선 검토하고, 불가피하게 라이브러리를 쓸 경우 그 이유를 명확히 한다.
- Node.js는 정적 사이트 생성(빌드 스크립트) 용도로만 사용 가능하며, 브라우저에 배포되는 결과물은 순수 HTML/CSS/JS여야 한다.

## 디자인 원칙

- 깔끔하고 읽기 좋은 타이포그래피 중심 디자인. 본문 가독성(줄 간격, 본문 폭, 여백)을 최우선으로 한다.
- 다크 모드 지원 필수. `prefers-color-scheme`를 기본으로 따르되, 사용자가 수동으로 토글할 수 있게 하고 선택값은 `localStorage`에 저장한다.
- 모바일 반응형 필수. 뷰포트가 좁아져도 레이아웃이 깨지지 않아야 하며, 코드 블록/표처럼 넓은 콘텐츠는 자체 스크롤을 갖도록 한다.
- 색상은 라이트/다크 각각 CSS 변수(custom properties)로 정의해서 관리한다.

## 작업 시 주의사항

- 새 기능이나 페이지를 추가하면 반드시 브라우저에서 라이트/다크 모드, 데스크톱/모바일 크기 모두 확인한다.
- 마크다운 변환 로직을 변경할 때는 코드 블록, 표, 이미지, 링크 등 주요 마크다운 요소가 깨지지 않는지 확인한다.
